node_modules/
dist/

# Produced from data-plane-service/src/main/proto before every build.
# Committing it would let it drift away from the contract it comes from.
src/generated/

# Produced from package.json, for the same reason: one source of truth.
src/version.ts
