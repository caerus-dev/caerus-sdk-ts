{
  "name": "@caerus-dev/sdk",
  "version": "0.1.0",
  "description": "Caerus SDK for Node.js — Shared Resource Engine client",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "npm run proto:generate && npm run version:generate",
    "proto:generate": "node scripts/generate-proto.mjs",
    "version:generate": "node scripts/generate-version.mjs",
    "clean": "node scripts/clean.mjs",
    "prebuild": "npm run generate",
    "build": "tsup",
    "postbuild": "node scripts/check-public-api.mjs",
    "pretypecheck": "npm run generate",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run generate",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.6"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.72.0",
    "ts-proto": "^2.6.1",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
